This folder is used for local pretrained weights.

Download the pretrained model from the LDP-Net official repository and place it here as:

```text
399_state.pth
```

The training config expects:

```yaml
pretrain_path: ./pretrain/399_state.pth
```

Pretrained weight files are ignored by Git.
